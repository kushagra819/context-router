# Virtual environment
venv/
.venv/
env/

# API keys and secrets
.env
.env.local
.env.production
.env.development
.env.staging

# Python caching
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/
.mypy_cache/

# Auto-downloaded datasets (large; fetched by `datasets` on demand)
data/

# Transient / throwaway experiment output
results/_mock/
*.prev.csv
results/routing/learned_router*.pkl

# NOTE: results/baselines/, results/baselines_backup/, results/routing/,
# results/routing_sim/, results/figures/, and results/*.{json,csv,md} ARE tracked
# on purpose — they are the truth source + reproducible artifacts.

# IDE files
.vscode/
.idea/
.DS_Store
